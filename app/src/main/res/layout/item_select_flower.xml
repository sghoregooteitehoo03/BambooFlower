<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="flower"
            type="com.sg.android.bambooflower.data.Flower"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="@drawable/shape_rounded_image"
        android:padding="8dp"
        app:setSelectFlower="@{flower.selected}">

        <ImageView
            android:id="@+id/flower_image"
            android:layout_width="66dp"
            android:layout_height="66dp"
            android:background="@drawable/shape_rounded_image"
            android:backgroundTint="@color/white_darker"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:setImage="@{flower.image}"
            tools:src="@drawable/ic_seed"/>

        <TextView
            android:id="@+id/flower_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:text="@{flower.name}"
            android:textColor="@color/default_item_color"
            app:layout_constraintEnd_toEndOf="@id/flower_image"
            app:layout_constraintStart_toStartOf="@id/flower_image"
            app:layout_constraintTop_toBottomOf="@id/flower_image"
            tools:text="대나무꽃"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>